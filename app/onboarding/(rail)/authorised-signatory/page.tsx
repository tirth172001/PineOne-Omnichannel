import { AuthorisedSignatoryClient } from "./_client"

// Authorised signatory step — first screen of the "KYC" macro-section
// (.scratch/onboarding-experience/map.md). Not yet linked from production navigation.
export default function AuthorisedSignatoryPage() {
  return <AuthorisedSignatoryClient />
}
