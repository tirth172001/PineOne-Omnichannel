import { StoreVerificationClient } from "./_client"

// Store verification step — third screen of the post-business-name onboarding sequence
// (.scratch/onboarding-experience/map.md). Not yet linked from production navigation.
export default function StoreVerificationPage() {
  return <StoreVerificationClient />
}
