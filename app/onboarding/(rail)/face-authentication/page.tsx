import { FaceAuthenticationClient } from "./_client"

// Face authentication step — second and final screen of the "KYC" macro-section
// (.scratch/onboarding-experience/map.md). Not yet linked from production navigation.
export default function FaceAuthenticationPage() {
  return <FaceAuthenticationClient />
}
